# Mosquitto MQTT broker configuration for ice-colder
#
# SECURITY WARNING: This config allows anonymous access for local development.
# For production deployment, switch to docker-compose.prod.yml which uses
# mosquitto-prod.conf with password authentication.
#
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type all
