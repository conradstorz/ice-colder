# This project uses pyproject.toml and uv for dependency management.
# This requirements.txt is for Docker/legacy compatibility.
# To update this file, run: uv pip compile pyproject.toml -o requirements.txt
# or use: uv export --no-dev > requirements.txt

# For Docker builds, you can install dependencies with:
# pip install -r requirements.txt
# or better yet: uv pip install -r pyproject.toml

fastapi[standard]>=0.115.11
uvicorn[standard]>=0.27.0
loguru>=0.7.3
pydantic>=2.10.6
pydantic-settings>=2.9.1
pydantic-extra-types>=2.10.4
pyserial>=3.5
qrcode>=8.1
pillow>=11.1.0
phonenumbers>=9.0.5
transitions>=0.9.2
ruff>=0.11.2
