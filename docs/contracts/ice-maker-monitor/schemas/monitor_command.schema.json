{
  "description": "VMC -> monitor command on cmd/ice_maker.",
  "properties": {
    "request_id": {
      "maxLength": 64,
      "minLength": 8,
      "title": "Request Id",
      "type": "string"
    },
    "command": {
      "enum": [
        "power_cycle",
        "force_report",
        "set_interval"
      ],
      "title": "Command",
      "type": "string"
    },
    "params": {
      "additionalProperties": {
        "type": "number"
      },
      "title": "Params",
      "type": "object"
    },
    "timestamp": {
      "format": "date-time",
      "title": "Timestamp",
      "type": "string"
    }
  },
  "required": [
    "request_id",
    "command"
  ],
  "title": "MonitorCommand",
  "type": "object"
}
