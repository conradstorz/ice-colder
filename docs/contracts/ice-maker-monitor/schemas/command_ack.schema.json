{
  "description": "Monitor -> VMC acknowledgement on cmd/ice_maker/ack.",
  "properties": {
    "request_id": {
      "description": "Echoed from the command",
      "title": "Request Id",
      "type": "string"
    },
    "command": {
      "title": "Command",
      "type": "string"
    },
    "status": {
      "enum": [
        "ok",
        "rejected",
        "failed",
        "unsupported"
      ],
      "title": "Status",
      "type": "string"
    },
    "detail": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Detail"
    },
    "timestamp": {
      "format": "date-time",
      "title": "Timestamp",
      "type": "string"
    }
  },
  "required": [
    "request_id",
    "command",
    "status"
  ],
  "title": "CommandAck",
  "type": "object"
}
