[project]
name = "ice-colder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.11",
    "loguru>=0.7.3",
    "pyserial>=3.5",
    "qrcode>=8.1",
    "ruff>=0.11.2",
    "transitions>=0.9.2",
]
